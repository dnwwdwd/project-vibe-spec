---
name: project-vibe-spec
description: 为 Vibe Coding 项目建立可追溯、经确认的协作契约。用于软件仓库的新建、修改、规划、排障、文档、测试、打包和交付；也用于执行 `init`，审计仓库现状、建立 AGENTS.md 任务路由、文档地图和有证据的交付基线。
---

# Project Vibe Spec

把用户意图、项目规则、产品/技术事实、设计决策、实现、验证和进度维持在同一条可追溯链路上。仓库已有规则优先沿用；模板只补缺失职责，不复制一套平行治理结构。

## 1. 项目治理分四层

后续工作按四层组织：

- **`AGENTS.md`：工作入口和路由。** 只保留所有任务都适用的边界、风险入口，以及“什么任务要读什么”的触发条件。
- **`DOCUMENT_MAP.md`：文档地图。** 把产品、技术、设计、需求、决策、进度等职责映射到仓库真实路径，并标记现行/参考/缺失/不适用。
- **事实与决策文档。** PRD、PDD、UI/设计规则、业务流程、DEC 等保存项目事实和长期约束。
- **交付记录。** REQ、Bug、项目进度和功能进度保存当前工作状态、验证证据和下一步。

不要把产品正文、数据库结构、接口字段、当前进度或临时需求堆进根 `AGENTS.md`。也不要要求每次任务读取所有项目文档；先从路由判断本次需要加载的上下文。

完整文档职责见 [references/document-maintenance.md](references/document-maintenance.md)。

## 2. `init`：建立或刷新项目基线

用户提出 `/init`、`$project-vibe-spec init`、初始化项目规范、刷新项目规则或重新梳理项目文档时，执行 [references/init-workflow.md](references/init-workflow.md)。

`init` 是可重复执行的审计命令：

1. 发现现有规则、文档、实现、测试和构建入口。
2. 按职责与状态更新 `DOCUMENT_MAP.md`。
3. 保留现有 `AGENTS.md`，补齐轻量任务路由；已有等价规则不重复创建。
4. 只创建当前确实需要且没有等价物的模板文件。
5. 基于文档、代码和验证证据建立交付基线。
6. 报告冲突、缺失事实和下一步。

PRD/PDD 缺失时先在地图中标记缺失。除非项目当前确实需要建立对应事实来源，否则不要为了“结构完整”自动创建空文档。代码只能证明当前实现，不能独立证明产品需求。

## 3. 每次任务先走路由，再确认需求

修改代码、配置、数据库、文档或 UI 前：

1. 读取当前目录适用的 `AGENTS.md`，再按其中路由读取本次任务相关的 `DOCUMENT_MAP.md` 条目、事实文档、需求/决策/进度和受影响代码测试。
2. 先理解现有业务：入口、主流程、状态流转、数据边界、权限边界和当前实现原因。
3. 分类请求：Bug 进入 Bug 台账；功能、功能优化和体验优化进入需求台账；仅重新构建或打包默认不建需求记录，除非项目另有约定。混合请求拆开记录。
4. 判断是否跨模块。同一持久化资源有多个写入入口或读取方，状态跨 API/SSE/缓存/后台任务传播，涉及 Agent、检索、索引、图片、文件、Skill、MCP，或涉及多端、权限、密钥时，按跨模块处理。
5. 跨模块非 Bug 需求建立详细 REQ，至少记录写入入口、读取方与刷新/恢复、失败/取消/回滚、安全/平台边界、已检查但不受影响的模块和行为验收矩阵。跨模块 Bug 仍进入 Bug 台账，但要记录影响范围和可复现/可验证行为。
6. 把目标、范围、不做项、验收、数据/权限/兼容影响和未知事项写入记录。描述完整且风险低时可继续；未知事项会改变产品范围、数据语义、权限、安全、兼容性或成本时，先获得用户明确答复。
7. 在确认边界内制定最小完整方案：改哪些层、复用什么、依赖、风险、验证和文档联动。存在多个合理方向或难以回退影响时，先讨论方案。

不能只读需求文本跳过代码，也不能在文档和实现冲突时静默猜测。

## 4. 数据与高风险决策单独过确认关卡

新增持久化数据、修改表/字段/索引/约束/关系、迁移或删除数据前，先调查现状，再给出可讨论的数据方案。未经确认，不修改 DDL、migration、ORM schema、数据访问代码，或依赖这些变化的 API/UI。

数据方案至少包含：业务语义、现有结构与读写依赖、可选方案、推荐理由、实体/字段/约束/索引、待确认 schema diff、迁移/回填/兼容窗口、性能、安全、备份和恢复。

改变产品范围、权限、安全、公开 API、兼容性、外部服务或其他难以回退事项，同样先走方案确认。详细清单见 [references/decision-gates.md](references/decision-gates.md)。

## 5. 大任务用 REQ + 功能进度推进

跨多个模块、多个阶段、需要多轮会话、涉及数据/迁移/外部系统，或无法在一个可验证闭环内完成的任务视为大任务。

1. 建立详细 REQ，写清范围、确认口径、依赖、验收和方案。
2. 沿用现有功能进度目录；没有等价物时使用 `Progress/PROG-<REQ-ID>-<slug>.md`。
3. 阶段写清目标、前置条件、交付物、验证证据、DoD、风险和下一步。
4. 阶段开始、完成或阻塞时同步功能进度与台账。
5. 如果项目总进度已存在，同步关联 REQ 的状态和证据；总进度不能单独宣称完成。

小任务通常只需要需求/Bug 台账和验证结果，不强制创建功能进度文件。

## 6. 在确认边界内实现，并维护事实来源

- 复用仓库已有抽象、依赖、命名和平台边界。保留无关用户改动，不顺手重构，不为未确认需求预建复杂能力。
- 先识别破坏性操作的精确目标，优先选择可恢复方式。密钥、环境变量、本地数据库、日志、依赖目录和构建产物默认不进版本控制。
- 实现改变了产品行为、技术约束、设计规则或业务流程时，更新对应的现行事实文档；没有变化时不要机械改文档。
- 新的长期稳定规则应该进入现有规则文档或模块级规则，再由根 `AGENTS.md` 添加触发路由；不要把长篇规则正文继续塞进根文件。
- 能由 lint、测试、类型检查或脚本稳定执行的规则，优先落实成自动化检查，文档保留触发条件和原因。

文档更新矩阵见 [references/document-maintenance.md](references/document-maintenance.md)。

## 7. 验证和交付以证据为准

按风险运行最小充分检查：聚焦测试、静态检查、构建，以及可行时的受影响用户路径。跨模块需求按行为验收矩阵回填。没有执行的检查不能写成通过。

真实平台、凭据或外部资源受限时，记录“待实机回归”的具体用户路径、原因和验证条件。lint、构建、静态断言或接口测试不能替代需要真实环境的行为验证。

已确认的数据迁移还要验证升级路径、约束/回填结果和失败恢复。不得使用生产数据作为验证环境。

暂存、提交或发布前检查工作区、分支、远程和忽略规则，只处理当前任务文件。push、部署、发布、合并和创建 PR 需要用户授权。

交付时说明：结果和关键决策、修改的代码与事实文档、REQ/Bug/进度记录、运行过的验证及结果、未验证部分、风险和仍需用户决定的事项。
