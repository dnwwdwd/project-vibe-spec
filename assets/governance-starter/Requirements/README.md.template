# Requirements

本目录记录非 Bug 需求。

- `LEDGER.md`：全部非 Bug 需求的总台账，记录状态、确认口径、决策和功能进度文档链接。
- `REQ-YYYYMMDD-NNN.md`：复杂、长期或跨模块需求的详细记录。

分类：功能需求、功能优化、用户体验优化。Bug 统一记录到 Bug 台账。

需求未完成澄清或方案确认时，状态保持为“待澄清”或“待方案确认”，不能用代码实现来替代确认。涉及数据、架构、公开 API、外部依赖或难以回退事项时，关联 `Decisions/DEC-*.md`。大任务还要关联 `Progress/PROG-*.md`。

同一持久化资源有多个写入入口或读取方，状态跨 API、SSE、确认卡、缓存或后台任务传播，涉及 Agent、检索、索引、图片、文件、Skill、MCP，或涉及平台、权限、密钥时，必须创建详细需求记录。记录写入入口、读取方与刷新/恢复、失败/取消/回滚和安全/平台边界、已检查但不受影响的模块，并附行为验收矩阵。

单文件、无持久化状态、无跨端影响的改动可只写简版台账。需求未完成影响分析、行为验收或相关文档同步时，不能标记“已完成”。真实环境受限时明确写“待实机回归”；静态检查、构建或接口测试不能替代用户路径验证。
