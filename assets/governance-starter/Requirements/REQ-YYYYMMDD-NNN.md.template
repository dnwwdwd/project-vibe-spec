# REQ-YYYYMMDD-NNN：<标题>

## 分类与状态

- 分类：功能需求 / 功能优化 / 用户体验优化
- 状态：待澄清
- 用户确认：未确认 / <日期、确认范围>
- 决策记录：`<实际路径；不需要时说明理由>`
- 功能进度文档：`<实际路径；小任务不需要时说明理由>`

## 背景与目标

<问题、目标、目标用户和预期结果。>

## 已确认需求

- 用户场景与主流程：
- 包含：
- 不包含：
- 权限、安全与数据边界：
- 兼容性与运行约束：
- 验收标准：

## 待确认问题

1. <会改变范围、数据、权限、兼容性或验收的问题；没有则写“无”。>

## 现状与受影响范围

- 现有实现与业务逻辑：
- 代码：
- 数据：
- UI：
- API/外部依赖：
- 文档：

## 跨模块影响分析

> 不适用时写明：单文件、无持久化状态、无跨端影响，并说明依据。

| 维度 | 已确认内容 |
|---|---|
| 写入入口 | API、页面、Agent Tool、确认卡、后台任务或其他实际入口。 |
| 读取方与状态传播 | 页面/控件、Prompt、缓存、SSE、重启恢复与刷新方式。 |
| 失败与边界 | 失败、取消、回滚、权限、密钥、平台和运行时差异。 |
| 已检查但不受影响 | 模块及不需要修改的原因。 |

## 方案与计划

### 可选方案

| 方案 | 做法 | 收益 | 代价/风险 |
|---|---|---|---|

### 推荐方案

<推荐项、具体理由、前置条件和需要用户确认的内容。>

### 实施步骤与验证

1. <步骤、依赖和验证方式。>

## 验收与结果

- [ ] 验收标准：
- 验证结果：
- 待实机回归：<用户路径、受限原因和验证条件；无则写“无”。>
- 未做项、风险或后续：

### 行为验收矩阵

| 场景 | 前置条件 | 操作 | 预期结果 | 验证状态 |
|---|---|---|---|---|
