# 文档地图

> 本表只负责导航。每项职责应指向仓库当前实际路径，并标记其状态。产品或技术结论写在对应事实文档中，不写在这里。

## 状态约定

- `现行`：当前权威来源。
- `参考`：历史、研究、草案或辅助资料，不作为现行约束。
- `缺失`：当前没有明确事实来源，不能自行补事实。
- `不适用`：项目明确不需要该职责，并写明原因。

## 当前映射

| 职责 | 类型 | 状态 | 实际路径 | 读取 / 更新触发条件 |
|---|---|---|---|---|
| Agent 工作入口 | 项目规则 | 现行 | `AGENTS.md` | 每次任务先读；全局规则或路由变化时更新 |
| 文档地图 | 导航 | 现行 | `DOCUMENT_MAP.md` | 文档正本、路径或状态变化 |
| 产品事实 / PRD | 产品事实 | 缺失 | - | 产品行为、范围、流程、权限口径或验收变化 |
| 技术事实 / PDD | 技术事实 | 缺失 | - | 架构、接口、数据、运行时或兼容变化 |
| 设计规范 | 设计事实 | 缺失 | - | UI、交互、样式、token 或文案变化 |
| 业务流程 | 产品/技术事实 | 缺失 | - | 跨模块业务、Agent、检索或数据流变化 |
| 需求台账 | 交付台账 | 缺失 | - | 新增或更新非 Bug 需求 |
| 详细需求记录 | 交付台账 | 缺失 | - | 跨模块、复杂或长期需求 |
| 决策记录 | 决策 | 缺失 | - | 数据、架构、API、外部依赖或难回退方案 |
| Bug 台账 | 交付台账 | 缺失 | - | Bug 发现、修复或验证 |
| 项目进度 | 交付台账 | 缺失 | - | 项目里程碑或交付基线变化 |
| 功能进度记录 | 交付台账 | 缺失 | - | 大任务阶段、证据、阻塞或下一步变化 |

## 初始化记录

- 最近审计日期：YYYY-MM-DD
- 沿用的既有规则/文档：
- 新建的缺失职责：
- 参考/历史资料：
- 待澄清冲突：
- 不适用职责及理由：
