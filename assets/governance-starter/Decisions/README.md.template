# Decisions

本目录记录需要用户确认的重大方案，尤其是数据设计、DDL/迁移、架构、公开 API、外部服务、权限、安全和难以回退的选择。

- `LEDGER.md`：决策状态与关联 REQ 的总台账。
- `DEC-YYYYMMDD-NNN.md`：单项方案、选项、用户确认和实施结果。

状态使用“待方案确认”“已确认待实施”“实施中”“已实施待验证”“已完成”“已取消”。在用户确认前，决策状态必须为“待方案确认”；方案文中的 DDL、schema diff 和迁移只作为草案，不能视为已授权执行。
