# 项目协作规范

本文件规定 Agent 在本仓库中的协作行为。产品、技术、数据和实施细节以 `DOCUMENT_MAP.md` 映射的项目文档为准，本文件不重复抄写。

## 文档索引与优先级

- 文档地图：[`DOCUMENT_MAP.md`](DOCUMENT_MAP.md)。开始任务前先读取它，按其中的实际路径定位需求、决策、进度和设计文档。它只负责导航，不记录产品或技术事实，也不改变本文件定义的优先级。
- 优先级：本文件 → 用户本次明确确认的需求或方案 → `DOCUMENT_MAP.md` 指向的需求/决策/产品/技术文档 → 进度文档 → 相关代码、测试、配置与脚本。
- 冲突时采用更高优先级规则；代码与文档不一致或存在会影响范围的未知事项时，记录差异并向用户确认，不能静默猜测。

## 首次接入

首次在本仓库使用协作规范时，检查现有文档和目录。已有近似职责的目录优先沿用，不建立平行台账；缺少的职责再创建。完成后更新 `DOCUMENT_MAP.md` 的真实路径，并保留本文件中的索引链接。

## 每次任务的确认流程

1. 阅读相关需求、决策、进度、产品、技术、UI、业务流程和受影响代码/测试，先理解当前业务主流程、数据和边界。
2. 分类请求：Bug 进入 Bug 台账；功能、功能优化和体验优化进入需求台账。同一持久化资源有多个写入入口或读取方，状态跨 API、SSE、确认卡、缓存或后台任务传播，涉及 Agent、检索、索引、图片、文件、Skill、MCP，或涉及平台、权限、密钥时，按跨模块处理并建立详细需求记录。
3. 跨模块非 Bug 需求的记录至少写明写入入口、读取方与刷新/恢复、失败/取消/回滚与安全/平台边界、已检查但不受影响的模块，以及行为验收矩阵。跨模块 Bug 仍只进入 Bug 台账，并记录影响范围、关联流程检查和可复现或可验证的用户行为。单文件、无持久化状态、无跨端影响的改动可以简版记录。
4. 向用户确认目标、范围、不做项、验收标准、数据/权限/兼容性影响和未决问题；描述完整且风险很低时，将确认口径写入记录后继续。会改变产品范围、数据语义、权限、安全、兼容性或成本的未知事项，必须获得用户明确答复。
5. 在确认口径内提出最小完整计划，写清改动层、依赖、风险、验证和文档联动。存在多种合理方案或难以回退影响时，先列出方案、推荐项和理由，待确认后实现。

## 数据与数据库变更

新增持久化数据、变更表/字段/索引/约束/关系、迁移历史数据或删除数据前，必须先出数据设计方案并和用户确认。方案包含现状与依赖、可选方案、推荐理由、实体/字段/约束/索引、待确认 DDL、迁移与回滚、安全和数据生命周期。

未经用户确认，不得修改 DDL、migration、ORM schema、数据访问代码，或把待选方案实现为 API/UI 行为。实施中若数据语义或影响范围变化，暂停并重新确认。

## 计划与进度

跨模块、多阶段、多轮会话、涉及数据/外部系统或无法在一个可验证闭环内完成的任务，必须创建功能进度文档。将其路径写入需求台账和功能进度台账；每个阶段开始、完成、阻塞时更新状态、证据、风险和下一步。需求最终完成后保留验证证据和遗留项。

## 实现、验证与 Git

- 只修改当前任务范围，保留无关用户改动；不顺手重构，也不为未确认需求预建复杂能力。
- 文档、决策和进度与实现同步更新。相关文档无需更新时，在需求或交付记录中写明理由。
- 按风险运行相关检查，如实报告结果；跨模块需求逐项回填行为验收矩阵。未完成影响分析、未设计行为验收，或只验证接口/源码存在时，不得标记完成。
- 真实平台、凭据或外部资源受限时，明确记录“待实机回归”的用户路径、受限原因和验证条件；lint、构建、静态断言或接口测试不能替代行为验证。
- 提交前检查工作区、分支、远程与忽略规则，只暂存本次工作。未经用户明确授权，不推送、发布、部署或创建 PR。
- 不提交密钥、`.env*`、本地数据库、备份、日志、依赖目录和构建产物，除非项目明确要求。
