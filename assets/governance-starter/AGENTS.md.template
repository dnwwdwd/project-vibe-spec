# 项目 Agent 入口

本文件只保存所有任务都适用的规则、风险入口和文档路由。产品、技术、设计、数据和当前进度以 `DOCUMENT_MAP.md` 映射的现行文档为准。

## 全局边界

- 开始任务前先检查工作区、当前分支和受影响代码，不覆盖、不回退用户已有改动。
- 优先沿用仓库现有约定、目录和工具；没有明确收益时不引入平行结构。
- 代码与文档冲突时记录差异。会改变产品范围、数据语义、权限、安全、兼容性或成本的事项交给用户确认。
- 未经明确授权，不执行 push、发布、部署、合并、删除数据或其他难恢复操作。
- 密钥、令牌、授权文件、本地数据库、日志和私有数据不得写入 Git 可跟踪路径。

## 文档入口

文档地图：[`DOCUMENT_MAP.md`](DOCUMENT_MAP.md)。

先根据任务类型读取相关现行文档，再读受影响代码和测试。不要每次加载全部项目文档。

<!-- project-vibe-spec:routes:start -->
## 任务路由

- 修改产品行为、用户流程、权限口径或验收标准：读取需求记录和产品事实来源。
- 修改 UI、交互、样式、设计 token 或用户可见文案：读取设计事实来源，并检查对应产品规则。
- 修改架构、接口、Agent、Skill、MCP、检索、索引、运行时或兼容行为：读取技术事实来源和关联决策。
- 修改数据库 schema、migration、持久化模型或数据生命周期：读取技术/数据规则和关联 DEC；任何 DDL 或迁移先完成方案确认。
- 修复 Bug：读取 Bug 台账、受影响模块规则、相关代码和测试；若 Bug 改变现行产品/技术事实，同步对应正本。
- 处理大任务：读取关联 REQ 和功能进度记录。
- 构建、打包、发布或部署：读取构建、发布、兼容和环境规则。
- 准备提交或交付：运行与风险匹配的验证，检查整体 diff，并如实记录未验证项。
<!-- project-vibe-spec:routes:end -->

## 实现与验证

- 先读实际代码和测试，再决定实现；文档只提供约束和导航。
- 修改保持在当前任务范围内，不顺手重构，不为未确认需求预建能力。
- 实现改变产品、技术、设计或业务流程事实时，更新对应现行文档；没有事实变化时不机械改文档。
- 能由 lint、测试、类型检查或脚本稳定保证的规则，优先落实到自动化检查。
- 完成后运行与风险匹配的检查，并报告已验证、未验证、风险和待用户决定事项。

## 模块级规则

只对单个目录或模块长期有效的规则，可以放到该目录的嵌套 `AGENTS.md`。仅在当前 Agent 运行时支持目录级规则继承时使用；不要批量生成。
